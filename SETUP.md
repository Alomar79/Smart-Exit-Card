# دليل الإعداد والتشغيل | Smart Exit Card

[العودة إلى تعريف المشروع](README.md)

يشرح هذا الدليل تشغيل نسخة مستقلة من المشروع باستخدام Google Sheets وGoogle Apps Script. رفع الأكواد إلى GitHub وحده لا يشغّل التطبيق؛ التشغيل يتم من Apps Script.

## 1. المتطلبات ونطاق النسخة

- حساب Google يسمح باستخدام Sheets وApps Script، ومتصفح واتصال بالإنترنت.
- نسخة جديدة من Google Sheets للتجربة، منفصلة عن بيانات المدرسة.
- ملفات المشروع الموجودة في هذا المستودع.
- بيانات وهمية للتجربة أو لأي عرض عام.

النسخة الحالية لا تتضمن تسجيل دخول داخل التطبيق أو فصل صلاحيات فعليًا بين المعلمة والمشرفة. اسم المعلمة حقل نصي، ومسار المشرفة ليس وسيلة حماية. عند إتاحة التطبيق للجميع، يستطيع من يصل إلى روابطه استخدام وظائفه وقراءة البيانات المعروضة. لا تنشر رابط التطبيق المدرسي أو رموز QR الفعلية ضمن المستودع العام.

لكل مجموعة بطاقة واحدة، ويسمح بخروج شخص واحد في الوقت نفسه من تلك المجموعة. يُمثَّل الصف المختلط بمجموعتين منفصلتين: طالبات بلون Pink وطلاب بلون Green.

## 2. إنشاء الملف ونقل الأكواد

1. أنشئ Google Sheet فارغًا وسمّه مثلًا Smart Exit Card Demo.
2. من داخله اختر **Extensions → Apps Script**.
3. سمِّ مشروع Apps Script.
4. انسخ محتوى ملفات المستودع إلى ملفات مطابقة داخل المحرر:

| النوع في المحرر | الملفات |
|---|---|
| Script — امتداد .gs | Code، Config، Setup، Verify، DataService، QueryService، CardService، Utils |
| HTML — امتداد .html | Index، ClientUtils، Scripts، Styles، QrLibrary، QrScripts، QrStyles، SchoolLogo |
| Manifest | appsscript.json |

عند إنشاء ملف، اكتب الاسم دون تكرار الامتداد الذي يضيفه المحرر. استبدل محتوى Code.gs الافتراضي بمحتوى ملف المشروع. لا تنقل README أو SETUP أو مجلد screenshots إلى Apps Script.

احتفظ بوسوم HTML وscript وstyle كما هي في الملفات، واحتفظ بإشعار ترخيص مكتبة QR.

## 3. إعداد ملف Manifest وخدمة Sheets

1. افتح **Project Settings** في Apps Script.
2. فعّل إظهار ملف **appsscript.json** في المحرر.
3. عد إلى المحرر، وافتح الملف ثم استبدل محتواه بمحتوى [appsscript.json](appsscript.json) في المستودع واحفظه.

هذا الملف يحدد V8 والمنطقة الزمنية Asia/Qatar، ويتضمن خدمة Google Sheets المتقدمة باسم Sheets وإصدار v4. طريقة إظهار الملف موضحة في [توثيق Google لملف Manifest](https://developers.google.com/apps-script/concepts/manifests).

تأكد من ظهور Sheets ضمن **Services**. إذا لم تظهر، أضف **Google Sheets API** من زر + بجانب Services، بالمعرّف Sheets وإصدار v4. لا تضف إدخالًا مكررًا إذا كانت الخدمة موجودة.

في مشروع Google Cloud الافتراضي الذي ينشئه Apps Script، تُفعّل API تلقائيًا عند إضافة الخدمة. إذا ربطت المشروع بمشروع Cloud قياسي، فعّل Google Sheets API فيه أيضًا. [توثيق الخدمات المتقدمة](https://developers.google.com/apps-script/guides/services/advanced)

## 4. تشغيل الإعداد والتحقق

1. احفظ جميع الملفات.
2. اختر الدالة **setupProject** من قائمة الدوال، ثم اضغط Run.
3. راجع أذونات حسابك وامنح المشروع الأذونات اللازمة لتشغيل نسختك.
4. انتظر انتهاء التنفيذ، ثم عد إلى Google Sheets.

تنشئ الدالة الأوراق المطلوبة وتربط السكربت بهذا الملف تلقائيًا عبر SPREADSHEET_ID في Script Properties؛ لا يلزم لصق معرّف ملف يدويًا في الكود.

عند الإعداد الأول لملف فارغ، تُضاف أربع مجموعات وأربع بطاقات وثمانية أسماء للتجربة وأربع وجهات. هذه بيانات أولية وليست بيانات الشعب الـ41 المذكورة في وصف المشروع.

شغّل **verifyProjectSetup** وافتح Execution log. النتيجة السليمة تتضمن:

```json
{
  "ok": true,
  "summary": "الإعداد سليم",
  "issues": []
}
```

إذا كانت ok تساوي false، صحّح الملاحظات في issues وأعد التحقق قبل الاستخدام.

هذا دليل تثبيت جديد. لا تشغّل الإعداد على ملف قديم دون نسخة احتياطية: قد يؤرشف أوراق Log أو Destinations غير المطابقة، ويتوقف عند وجود خروج قديم فعال أو أعمدة غير مطابقة في أوراق أخرى. لا يحوّل كل بيانات النظام القديم تلقائيًا.

## 5. فهم الأوراق وإدخال البيانات

لا تغيّر أسماء الأوراق أو عناوين الأعمدة أو ترتيبها، ولا تضف أعمدة داخل جداول النظام.

| الورقة | الوظيفة |
|---|---|
| FemaleStudents | بيانات الطالبات وربطهن بالمجموعات الوردية |
| MaleStudents | بيانات الطلاب وربطهم بالمجموعات الخضراء |
| Groups | أسماء المجموعات ولون كل مجموعة وحالتها |
| Cards | ربط بطاقة واحدة بكل مجموعة |
| Destinations | الوجهات المتاحة لكل لون أو للجميع |
| Log | سجل الخروج والعودة الذي يكتبه التطبيق تلقائيًا |
| Settings | إعدادات الاسم والتوقيت وحدود الألوان والتحديث |

### مثال لإضافة صف جديد

أضف في Groups:

| GroupID | GroupName | CardTheme | Status |
|---|---|---|---|
| C301-F | طالبات ثالث1 | Pink | Active |
| C301-M | طلاب ثالث1 | Green | Active |

ثم أضف في Cards:

| CardID | GroupID | Status |
|---|---|---|
| CARD-C301-F | C301-F | Active |
| CARD-C301-M | C301-M | Active |

أضف في FemaleStudents:

| StudentID | StudentName | GroupID | Status |
|---|---|---|---|
| FS101 | طالبة تجريبية 1 | C301-F | Active |

وأضف في MaleStudents:

| StudentID | StudentName | GroupID | Status |
|---|---|---|---|
| MS101 | طالب تجريبي 1 | C301-M | Active |

استخدم معرّفات فريدة داخل كل ورقة، من حروف إنجليزية وأرقام وشرطة أو شرطة سفلية، دون مسافات، وبحد أقصى 100 محرف. يجب أن يطابق GroupID القيمة في Groups تمامًا.

كل مجموعة تحتاج بطاقة واحدة فقط، حتى عند وجود بطاقات غير مفعلة؛ لا تنشئ بطاقة ثانية للمجموعة نفسها. استخدم Active للتفعيل وInactive للتعطيل. سجّل عودة أي شخص خارج الصف قبل تعديل معرّفات مجموعته أو بطاقته أو حذفها.

### إضافة وجهة

في Destinations، أضف معرّفًا فريدًا واسمًا وAvailability بقيمة Pink أو Green أو All، وحالة Active. مثال:

| DestinationID | DestinationName | Availability | Status |
|---|---|---|---|
| D005 | المكتبة | All | Active |

اسم المعلمة يُدخل في واجهة الخروج؛ لا توجد ورقة معلمات أو حاجة لإنشاء بطاقة لكل معلمة في هذه النسخة.

### الإعدادات

عدّل عمود Value أمام المفتاح المطلوب في Settings:

| Key | الافتراضي | المعنى |
|---|---|---|
| SCHOOL_NAME | مدرسة السلم الخامسة | الاسم الذي تقرؤه الواجهة من الإعدادات |
| TIMEZONE | Asia/Qatar | التوقيت المدعوم في هذه النسخة |
| GREEN_LIMIT | 10 | حد اللون الأخضر بالدقائق |
| ORANGE_LIMIT | 15 | حد اللون البرتقالي بالدقائق |
| AUTO_REFRESH_SECONDS | 30 | فترة التحديث الدوري بالثواني |

يجب أن يكون GREEN_LIMIT أكبر من صفر، وORANGE_LIMIT أكبر منه، والتحديث 5 ثوانٍ على الأقل. لا تغيّر TIMEZONE؛ الكود الحالي يعتمد توقيت قطر في معالجة التواريخ أيضًا.

تعديل SCHOOL_NAME لا يستبدل تلقائيًا كل هوية المدرسة: يوجد عنوان ثابت في Code.gs وشعار مضمّن في SchoolLogo.html وQrScripts.html.

بعد تعديل البيانات أو Settings، شغّل **verifyProjectSetup** ثم **clearDataCache** وحدّث صفحات التطبيق. قد تبقى القوائم مخزنة مؤقتًا حتى 180 ثانية دون مسح الذاكرة.

## 6. نشر نسخة تجريبية

1. من Apps Script اختر **Deploy → New deployment**.
2. اختر النوع **Web app**.
3. اكتب وصفًا مثل Initial demo.
4. اختر **Execute as: Me**.
5. للتجربة دون تسجيل دخول ببيانات وهمية، اختر **Who has access: Anyone** إذا كان حسابك يسمح بهذا الخيار.
6. اضغط Deploy وأكمل التفويض عند طلبه.
7. انسخ رابط Web app الذي ينتهي بـ **/exec**.

ينفذ خيار Me العمليات بصلاحيات ناشر التطبيق. قد تختلف خيارات الوصول حسب حساب Google وسياسة المؤسسة. لا تجعل ملف Google Sheets عامًا؛ الوصول إلى التطبيق مختلف عن مشاركة الجدول. [توثيق نشر تطبيقات الويب](https://developers.google.com/apps-script/guides/web)

استخدم رابط /exec للمستخدمين. رابط /dev مخصص للاختبار بواسطة من لديهم حق تعديل السكربت.

## 7. فتح الصفحات وبطاقات QR

اعتبر BASE_URL رابط تطبيقك الكامل المنتهي بـ /exec. استبدله فعليًا بالرابط؛ لا تكتب BASE_URL حرفيًا في المتصفح.

| الصفحة | الرابط |
|---|---|
| الرئيسية | BASE_URL |
| لوحة المشرفة | BASE_URL?view=supervisor |
| طباعة بطاقات QR | BASE_URL?view=qr |
| بطاقة الطالبات الأولية | BASE_URL?view=card&cardId=CARD-C201-F |
| بطاقة الطلاب الأولية | BASE_URL?view=card&cardId=CARD-C201-M |

المثالان الأخيران يعملان ما دامت البطاقتان الأوليتان موجودتين ومفعّلتين. للصف الجديد استخدم CardID الخاص به. افتح صفحة QR من نسخة /exec المنشورة، وتحقق من الوجهة التي يفتحها المسح قبل طباعة البطاقات.

## 8. اختبار الخروج والعودة

1. افتح بطاقة وردية وتأكد أنها تعرض طالبات المجموعة فقط.
2. اكتب اسم معلمة تجريبيًا، واختر طالبة ووجهة ثم سجّل الخروج.
3. افتح لوحة المشرفة وتأكد من ظهور الاسم والمجموعة والوجهة.
4. تحقق من تسجيل صف بحالة ACTIVE في Log.
5. جرّب فتح البطاقة نفسها من نافذة أخرى؛ لا يفترض أن تسمح بخروج شخص ثانٍ قبل العودة.
6. سجّل العودة من لوحة المشرفة؛ الزر ينفذ العودة مباشرة.
7. تحقق من اختفاء السجل من قائمة الموجودين بالخارج وتحوّل الحالة إلى RETURNED وتعبئة وقت العودة والمدة في Log.
8. كرر الاختبار مع بطاقة خضراء، وامسح QR من هاتف لاختبار الرابط.

نجاح verifyProjectSetup يفحص بنية البيانات، لكنه لا يغني عن اختبار التطبيق المنشور وخدمة Sheets والصلاحيات. لا يحتاج تسجيل الخروج والعودة إلى تشغيل دوال يدويًا من المحرر.

## 9. التعديلات اللاحقة

| التغيير | الإجراء |
|---|---|
| إضافة طالب أو مجموعة أو وجهة، أو تعديل Settings | تعديل Sheets ثم التحقق ومسح الذاكرة وتحديث الصفحة؛ لا يحتاج نشرًا جديدًا |
| إضافة مجموعة جديدة | إضافة المجموعة وبطاقتها والطلبة المرتبطين بها، ثم تحديث صفحة QR |
| تعديل ملفات .gs أو .html أو Manifest | حفظ الأكواد ثم Deploy → Manage deployments → Edit → New version → Deploy |
| تحديث ملفات GitHub فقط | لا يحدّث Apps Script تلقائيًا؛ انقل التعديلات إلى مشروعه ثم حدّث النشر |
| إنشاء نسخة جديدة من Google Sheet | تحقق من ارتباط السكربت بالنسخة، وشغّل setupProject وverifyProjectSetup ثم انشر تطبيق النسخة |

حدّث النشر الموجود للحفاظ على رابطه. إذا أنشأت نشرًا مستقلًا برابط جديد، يلزم استخدام الرابط الجديد في بطاقات تلك النسخة.

## 10. مشكلات شائعة

| المشكلة | ما ينبغي مراجعته |
|---|---|
| Run setupProject in the bound spreadsheet first | افتح Apps Script من Google Sheet المقصود وشغّل setupProject |
| Sheets is not defined | خدمة Sheets المتقدمة ومعرّفها وإعداد Manifest |
| Missing sheet أو Invalid headers | أسماء الأوراق وعناوين الأعمدة حسب Config.gs |
| البطاقة غير موجودة أو غير مفعلة | CardID وارتباطه بالمجموعة وحالة كليهما |
| لا تظهر بطاقة جديدة | وجود صف Cards للمجموعة، والتفعيل، وعدم تعدد بطاقات المجموعة؛ ثم clearDataCache وتحديث صفحة QR |
| لا تظهر الأسماء الجديدة | الورقة المناسبة للون، وGroupID وActive، ثم clearDataCache وتحديث الصفحة |
| يعمل الرابط للمالك فقط | إعداد الوصول في النشر، واستخدام /exec، وسياسة الحساب |
| التعديلات البرمجية لا تظهر | اختيار New version في النشر الموجود ثم تحديث المتصفح |
| يوجد خروج قديم فعال أثناء الإعداد | تسجيل العودة في النظام القديم أولًا؛ لا تحذف سجل الخروج لتجاوز الفحص |

[العودة إلى README](README.md)

